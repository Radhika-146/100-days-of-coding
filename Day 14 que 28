//Write a program to print the sum of the first 1 to n even numbers.

#include <stdio.h>
int main()
{
    int n,sum=0;
    printf("enter the num");
    scanf("%d",&n);
    for (int i=1;i<=n;i++)
    {
          sum=sum+(2*i);  
    }
    printf("%d",sum);
    return 0;
}
