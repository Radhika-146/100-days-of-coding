Q12: Write a program to input an integer and check whether it is positive, negative or zero using nested if–else.

#include <stdio.h>

int main() {
    float n1;
    printf("enter a number");
    scanf("%f",&n1);
    if (n1>0)
    {
        printf("num is positive %f",n1);
    }
    if (n1<0)
    {
        printf("num is negative %f",n1);
    }
    if (n1==0)
    {
        printf("num is zero %f",n1);
    }
    return 0;
}
