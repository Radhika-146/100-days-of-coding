#include <stdio.h>
#include <string.h>
int main()
{
    char date[50];
    printf("enter the date in format of dd/mm/yyyy");
    scanf("%s",date);
    int len=strlen(date);
    for (int i=0;i<len;i++)
    {
        if (date[i]=='/')
        {
            date[i]='-';
        }
    }
    printf("%s",date);
    for (int j=3; j<5;j++)
    {
        if ()
    }
    return 0;
}
