//Write a program to find and display the sum of the first n natural numbers.

#include <stdio.h>
int main() {
    int n;
    printf("enter the number of terms");
    scanf("%d",&n);
    int s;
    int c=n+1;
    s=(n*c)/2;
    printf("sum of first natural numbers%d",s);
    return 0;
}
