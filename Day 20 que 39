//Write a program to find the product of odd digits of a number.

#include <stdio.h>
int main()
{
   int n;
   printf("Enter number:");
   scanf("%d",&n);
   int p=1;
   while(n!=0)
   {
     int d=n%10;
     if(d%2!=0)
     p=p*d;
     n=n/10;
  }
  printf("Product = %d ",p);
  return 0;
}
